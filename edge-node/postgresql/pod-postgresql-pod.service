# pod-postgresql-pod.service
# autogenerated by Podman 3.3.1
# Tue Dec 21 03:32:09 EST 2021

[Unit]
Description=Podman pod-postgresql-pod.service
Documentation=man:podman-generate-systemd(1)
Wants=network-online.target
After=network-online.target
RequiresMountsFor=
Requires=container-postgresql.service container-postgresql-exporter.service
Before=container-postgresql.service container-postgresql-exporter.service

[Service]
Environment=PODMAN_SYSTEMD_UNIT=%n
Restart=on-failure
TimeoutStopSec=70
ExecStartPre=/bin/rm -f %t/pod-postgresql-pod.pid %t/pod-postgresql-pod.pod-id
ExecStartPre=/usr/bin/podman pod create --infra-conmon-pidfile %t/pod-postgresql-pod.pid --pod-id-file %t/pod-postgresql-pod.pod-id --name postgresql-pod --replace
ExecStart=/usr/bin/podman pod start --pod-id-file %t/pod-postgresql-pod.pod-id
ExecStop=/usr/bin/podman pod stop --ignore --pod-id-file %t/pod-postgresql-pod.pod-id -t 10
ExecStopPost=/usr/bin/podman pod rm --ignore -f --pod-id-file %t/pod-postgresql-pod.pod-id
PIDFile=%t/pod-postgresql-pod.pid
Type=forking

[Install]
WantedBy=multi-user.target default.target
